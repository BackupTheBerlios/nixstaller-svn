OVERALL
- Add trap in geninstall.sh which cleans stuff?
- Use deltas <--> patches for different frontends, OS'es, archs
- Modify Makeself
    - Remove some illogical commandline options (finished?)
- libstdcxx:
    - Compile NetBSD x86_64 bins on 3.X instead of 4.X (update notes)
    - Update FreeBSD notes file for for libsupc++ (used from 6.X)
- Test regular C lua libs, update notes
- deps:
    - Check for static bins?
    - Better progress indication --> Remove any (debug) timeouts
    - Additional wrongdeps/wronglibs checks?
- Check desktopentries in Ubuntu and destdir for SelectDirScreen
- Dep templates:
    - For list: LSB Compliance, others? --> LSB tag?
    - More templates?
    - Test templates and check if they can be full and need extra code for being full (eg fontconfig, freetype) (finished?).
    - Add description for all options (finished?)
    - Check for messages using 'library' and change (finished?)
- Nicer way to run apps on exit. Or atleast document it
- Installer profiles?
- Check messages like pkg enable screen which refer to a default
- Unattended installations
    - Check for required arguments
    - Reset cfg.unopts[].value? if not document for defaults
    - Control verbosity?
    - Make --destdir option required? set default?
- Move curdir lua variable to install package?
- Mail suggestions?
- Move pkg.verifydeps to install package? (If yes modify genprojdir.sh)


TEST
- MD5 code for instheader.sh, makeself.sh and generic packages.


PACKAGING
- RPM: Description formatting?
- pacman: Check for locks for future releases
- deb: way to remove temporary root owned files incase of an error
- Check for deb to dpkg changes (lock translations)


MAIN
- Improve/check lua error messages?
- Make startinstall code more flexible
    - Check if all lua functions from install package need to be inside Install() (update docs)
    - Test translations
    - Re-enable SU passwd cleaning at end of install?
    - Make new lua functions 'public'?
- Change PATH handling for ExecuteCommand/ExecuteCommandAsRoot to use user's/root $PATH


DOCS
- utils.tablefind, utils.tablemerge, utils.mapsize and the rest from CHANGELOG
- install.getpkgdir() (now with arg)
- utils.removerec()
- utils.mkdirperm()? (If yes, CHANGELOG)
- Nested block strings ([=[]=])
- install.update()? (If yes update CHANGELOG)
- addcheckbox + addradiobutton + addmenu functions now have additional arg for default enabled option(s)
- add/del options for menu, radiobutton, checkbox
- menu, radiobutton, checkbox can now be empty
- del() function for luacfgmenu
- GTK/Pango templates
- Apple stuff
- utils.emptystring()? (CHANGELOG)
- Unattended installs (cfg.unopts, cfg.unattended etc)
- verifydeps uses setstatus
- os.geteuid()? (CHANGELOG)
- utils.tableappend()


RELEASE
- Remove/disable asserts in lua code


TRANSLATIONS
- Check all sentences for punctation
- Change "Install commands ..." cancel message to something more appropiate --> Update translations
- verifylock() messages
- "Reverting to generic package."
- Check unattended installs
- utf8:
    - Check if FLTK 1.3 is usable enough (or will be soon)
    - Fix ncurses and perhaps others for width calculating
    - Convert language files to utf?
    - Convert utf to native for ncurses frontend?
- Neater tr() function? Document it?

LIBSU
- Check tmp files leaving around (caused by sh errors?)


NCURSES


GTK


FLTK
- Check update: test for regressions, check current hacks.
- Progressdialog: Refresh does not always enable both bars (FLTK specific?)
- Check why Text Buffer appending is slow (Fixed with 1.3?)


DESIGN


-----------------------------------------------------------------------
LATER
-----------------------------------------------------------------------

OVERALL
- Qt/KDE frontend
- Fix installation if destdir has no read access?
- Use locale fix from latest kdesu?
- Better util.a configure testing?
- Root usage: change to use privileged user? (default root)
- deps:
    - bins field, which checks for bin compat? If yes change deputil.lua HandleCompat function generation and geninstall autosyms.

PACKAGING
- More config options:
    * Package Changelog?
    * RPM: Vendor?, Icon?


MAIN
- More advanced ExtractFiles() (Work as a Lua iterator? Define which package to extract)


LIBSU
- Clean up/redesign


NCURSES
- Check chtype?
- Better monochrome support?
- Check for 'dynamic' CanFocus for CTextField?


GTK


FLTK
- Create a pack widget that works?


DESIGN
- Reorganize SConstruct
