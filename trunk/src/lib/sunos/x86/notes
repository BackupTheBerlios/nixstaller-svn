Notes for solaris libs:

Host system: x86 Solaris 10-4

For library versions and build options see builddep.sh. Note that some dependencies will give build errors, however the required files will already be build and can be copied manually.