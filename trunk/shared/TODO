- Let Lua code use MoveFile()
- Clean exception code?:
    - Don't group all classes in same header?
    - Use something else as char buffers (std::string?, msg table?, force valid char*?)
    - Translate somewhere else? Translate at all?
    - Store source/line for debugging? Backtraces?
- Pseudo terminals:
    - Test on all OSs
    - CHANGELOG
    - Test process killing
    - Test custom path's
- Fix O_CREAT mode flags for file copy
    